development:
    dialect: mysql
    datasource: userDev:passDev@/expenses?parseTime=true
    dir: migrations/mysql
    table: migrations

production:
    dialect: postgres
    datasource: dbname=myapp sslmode=disable
    dir: migrations/postgres
    table: migrations

#    -env = production